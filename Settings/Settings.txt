# System settings
logLevel	2				# int: log verbose level of Tracking (0-4)
speciesName	'Clownfish'		# String: name of the fish species to be tracked
imgMode		'crop'			# string: image monitoring mode: 'full' or 'crop'
xMonitWin	300				# int: x coordinate of left edge of monitoring window (on linux: 340, on windows: 320)
resultsDir	'D:\Results'	# String: results directory, ssd or devs '/disks/ssd/Results' or 'E:\Results'
expID		'CP_FVAPV_clowns'	# string: expID, use '' if you want to use menu input
ext2D		'_DLC2D (new)'	# string: file extension for 2D results (ex. '_pos2D+DLC2D' or '_pos2D+DLC (new)')
ext3D		'_DLC3D (new)'	# string: file extension for 3D results (ex. '_pos3D+DLC3D' or '_pos3D+DLC (new)')
saveVideos	False			# bool: save output videos with results overlay
sendPos3D	False			# Bool: send 3D positions to Unreal based on detection or DLC (needs triangulate enabled)

# Viewer settings
viewMode	3				# int: viewer mode 2:'2D videos' or 3:'3D plots'
showDLC		True			# bool: show DLC
showTrack	True			# bool: show animal tracking
sizeToProba	True			# bool: size DLC markers with sqrt of inferred proba (by steps of 1 pixel)
trailFrames	30				# int: number of frames shown for trail (from 1 no trail to 60 for a 2s trail)
useCyclop	True			# bool: use 2D detection if True, else DLC inferred cyclop
showVelocity	True		# bool: show cyclop velocity if True, needs cyclopVel in the data
showGazeDir	True			# bool: show gaze direction if True, needs gazeDir in the data
speed		1				# float: play speed, use values in [0.125, 0.25, 0.5, 1, 2, 4, 8]

# Camera settings
camList		[0, 1]			# list of int: cameras to use, should be [0, 1] or [0]
cropSize	(500, 500)		# tuple: size of the ROI for the crop
cropULs		[(390, 262), (390, 262)]	# List of UL position for each camera index (ROI definition)
rotateCamList	[90, -90]	# list of floats: camera rotations to align monitoring with setup
